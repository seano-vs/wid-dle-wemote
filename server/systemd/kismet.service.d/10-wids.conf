[Unit]
# Radios must be in monitor mode before Kismet opens them.
After=wids-monitor.service
Requires=wids-monitor.service

[Service]
Restart=always
RestartSec=5
# Kismet must never write capture data to disk on this rig.
ReadOnlyPaths=/var/log
